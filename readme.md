# Webtonative.app

## Development setup

1. Clone the repo
2. Install the gems.
    ```bash
    bundle install
    ```
3. Run the Jekyll server
    ```bash
    jekyll serve
    ```
4. Commit and push to master.